//find pair with sum in the array if possible return true else false 
#include<iostream>
#include<algorithm>
using namespace std;
int pairo(int arr[],int n,int sum)
{
	int i,j=n-1;
	// sort the elements in the array
    sort(arr,arr+6);	
	while(i<n)
	{
		if(arr[i]+arr[j]==sum)
		{
			return 1;
		}
		else if(arr[i]+arr[j]>sum)
		{
		j--;
		}
		else if(arr[i]+arr[j]<sum)
		{
			i++;
			
}
		
	}
	
}
int main()
{
	int arr[]={11,15,26,38,9,10},sum=45;
	int n=sizeof(arr)/sizeof(arr[0]);
	if(true)
	{
		cout<<"true";
	}
	else
	{
		cout<<" false";
	}
	return 0;
}















































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































{
//	int arr[]={11,15,6,8,9,10},sum=16,n;
	int arr[]={11,15,26,38,9,10},sum=35,n;
	n=sizeof(arr)/sizeof(arr[0]);
	pairo(arr,n,sum);
	
	return 0;
}